# =============================================================================
# Velogen API 환경변수 설정
# 이 파일을 복사해 .env로 사용하세요: cp .env.example .env
# =============================================================================

# =============================================================================
# 공통 설정
# =============================================================================

# 프롬프트 최대 글자 수 — 초과 시 compact 모드로 자동 전환 (기본: 32000)
PROMPT_MAX_CHARS=32000


# =============================================================================
# AI 에이전트 설정
# Generator 드롭다운에서 선택한 프로바이더에 해당하는 항목만 설정하면 됩니다.
# 동시에 하나의 프로바이더만 사용됩니다.
# =============================================================================

# -----------------------------------------------------------------------------
# Claude (Anthropic Claude Code CLI)
# 드롭다운: "🧠 Claude (claude-code CLI)"
#
# 설치: npm install -g @anthropic-ai/claude-code
# 확인: claude --print "hello"
#
# MCP 연동 방법:
#   1. Claude Desktop 방식 (전역): ~/.claude/claude_desktop_config.json 수정
#   2. 프로젝트 방식: 프로젝트 루트에 .mcp.json 파일 생성
#
#   .mcp.json 예시:
#   {
#     "mcpServers": {
#       "filesystem": {
#         "command": "npx",
#         "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/allowed/dir"]
#       },
#       "github": {
#         "command": "npx",
#         "args": ["-y", "@modelcontextprotocol/server-github"],
#         "env": { "GITHUB_PERSONAL_ACCESS_TOKEN": "ghp_..." }
#       }
#     }
#   }
#
#   velogen API 서버 재시작 없이 .mcp.json 수정만으로 다음 생성부터 반영됩니다.
# -----------------------------------------------------------------------------
CLAUDE_COMMAND=claude
# CLAUDE_MODEL=claude-opus-4-5

# -----------------------------------------------------------------------------
# Codex (OpenAI Codex CLI)
# 드롭다운: "⚡ Codex (OpenAI Codex CLI)"
#
# 설치: npm install -g @openai/codex  (또는 공식 문서 참고)
# 확인: codex --approval-mode full-auto --quiet "hello"
# 필수: OPENAI_API_KEY 환경변수 설정
# -----------------------------------------------------------------------------
CODEX_COMMAND=codex
# CODEX_MODEL=o4-mini
# OPENAI_API_KEY=sk-...

# -----------------------------------------------------------------------------
# Opencode
# 드롭다운: "🛠 Opencode"
#
# 설치: 공식 문서(https://opencode.ai) 참고
# 확인: opencode run "hello"
# -----------------------------------------------------------------------------
OPENCODE_COMMAND=opencode
# OPENCODE_MODEL=anthropic/claude-sonnet-4-5

# -----------------------------------------------------------------------------
# Gemini (Google Gemini CLI)
# 드롭다운: "🌟 Gemini"
#
# 설치: npm install -g @google/gemini-cli
# 확인: gemini
# -----------------------------------------------------------------------------
GEMINI_COMMAND=gemini
# GEMINI_MODEL=gemini-2.5-pro

# (web) API 서버 기본 URL — NEXT_PUBLIC_ 접두사 필수
# NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
